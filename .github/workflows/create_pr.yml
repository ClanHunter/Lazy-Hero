name: Create PR (manual)

on:
  workflow_dispatch:

jobs:
  create_pr:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Debug / Placeholder
        run: |
          echo "Manual PR creation workflow (temporarily simplified to avoid YAML syntax issues)."
          echo "Restore full create_pr script from original commit when ready."
